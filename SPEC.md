# Curio · 好奇心收藏

> 一个把每天好奇过、想过、看过的东西，扔进去就行的 AI 二脑。
> 它替你打理分类、隔三差五提醒你「上周的你想过这个」，让你三个月后真切看到自己长进了。

- **代号**：Curio
- **Tagline（候选）**：a home for everything you're curious about.
- **作者**：Yuri（yuri@greaterheat.com）
- **建档日期**：2026-05-24
- **版本**：v1.1 — 2026-05-28 多用户开启 + 反思下线

## 重大变更（按时间倒序）

> 本文档是 v1.0 立项时的产品定义。下面这些功能后来被实际放弃了——下文相关章节会用 **⚠️ [已废弃]** 标记，但章节内容保留供决策回顾，不要按它去复刻。

| 日期 | 变更 | 影响章节 | 原因 |
|---|---|---|---|
| 2026-05-28 | 「今晚反思」全功能下线（页面/cron/lib/数据表全删，0005 migration drop） | § 3.2 旅程 B、§ 5 反思三态、§ 6 反思相关数据流 | 每日 LLM 成本与维护负担与价值不匹配；用户用了一段时间发现不会主动回看 |
| 2026-05-28 | 登录从 magic link 切到邮箱+密码 | § 8.1「账号」段 | 企业邮箱（byteark.cn 等）默认把 Supabase 发件归 spam，magic link 到达率不稳；密码登录可控且日常无邮件 |
| 2026-05-28 | 多用户真登录已开 + 生产种子用户兜底硬关 | § 8.1、§ 12 路线图 | 实际从自用切到可分享，每人 RLS 隔离的独立私库 |
| 2026-05-27 | 周报 / 月报 / 知识图谱 全删 | § 5、§ 6 相关章节 | 周报：用户判定没用；图谱：demo 惊艳但没有真概念关联，跟复利感目标不搭 |

---

## 1. 为什么做这个产品

### 1.1 起点

我每天会好奇很多事情：刷到一句广告标语、走进一家有趣的店、突然冒出一个产品 idea、看到一段写得好的文案、debug 学到一招、跟同事聊到一个洞察……

这些东西我现在散落在 4 个地方：手机相册（截图+随手拍）、微信收藏（链接+图）、备忘录（一两句话的想法）、飞书（工作相关）。

**问题不是"记不下来"，问题是：**
- 记下来了**永远不会回去看**
- 看了也**搜不到**关键的那一条
- **看不到自己在长进**

我想做一个能解决这三个的产品。

### 1.2 北极星指标

**用户用 3 个月后，能脱口而出「我在 X 主题上明显比 3 个月前想得更深了」。**

不是「我攒了 200 条笔记」，不是「整理得真干净」—— 是**感觉到自己真的复利了**。

这一句话否决一切其他设计目标。如果一个功能不能为这件事服务，砍。

### 1.3 用一句话定位

> Curio 不是笔记软件，是一个**会主动帮你打理、反思、回望的好奇心收藏馆**。

竞品参照（但都只覆盖一部分）：

| 竞品 | 它做对的事 | Curio 不一样在哪 |
|---|---|---|
| Mymind | 多模态捕获 + AI 自动打标签 | 没有反思层、没有复利可视化 |
| Reflect / Mem | AI 笔记 + 自动关联 | 仍要主动写笔记，不是被动捕获 |
| Readwise Reader | 每日回顾、复利感强 | 只服务「读」这一个场景 |
| Day One | 晚间反思仪式做得好 | 纯日记，没有知识沉淀和搜索 |
| Heptabase | 视觉化知识图 | 重度学者向，门槛高 |

Curio 要把 **多模态捕获 + AI 自动整理 + 每晚反思 + 复利可视化 + 语义搜索** 缝在一起，且不让人嫌烦。

---

## 2. 用户画像（V0 = 一个用户，作者本人）

### 2.1 Yuri · 初级 AI PM

- 身份：在 GreaterHeat 做 AI 产品的 PM
- 关心的事：每天接触很多信息、灵感、产品观察、设计参考、行业洞察
- 技术栈：Next.js / Vercel / DeepSeek / Tailwind / Zustand / Lark
- 工作语境：飞书生态、夜里和周末会自己做 PoC
- 偏好：短句中文沟通、不爱"好的"开头、追问背后原因、写作口语化但不随意
- 风险偏好：愿意为长期个人产品多花一点心思，但 V0 要在 2-3 周内能用

### 2.2 V1 之后的目标用户（商业化考虑预留架构）

- AI 圈、设计圈、产品圈的「好奇心驱动」专业人士
- 同时使用 5+ 信息收集渠道、苦于"记了不复用"的人
- 不要做给重度 PKM 极客（不是 Roam/Tana 群体）

---

## 3. 三类核心用户旅程

### 3.1 旅程 A：捕获（白天，多次发生）

```
触发    →  操作                              →  系统行为
─────────────────────────────────────────────────────────
看到一  →  iOS 分享 → Curio 快捷指令         →  上传到 Curio
张图       OR 主屏幕打开 PWA 拍照/截图        →  AI 自动 OCR + 内容理解
                                              →  AI 建议主题 + 1 句简介
冒灵感  →  打开 PWA / 飞书 bot                →  AI 推主题、写一句
说一句     文字 OR 录音                       →  Item 入库
                                              →  返回："收到，归到「XX」主题"
                                              →  捕获后第一屏：1 句话批注框
                                                 (语音/文字都行，1 秒能跳过)
```

**关键设计**：
- 捕获后的第一屏 **必有** 一句话批注框 + 录音按钮（不强制，但放在显眼位置）
- AI 提供「这条像是 XX 主题」的标签建议，**一键确认**入库
- 如果用户不批注、不确认，**系统自动用 AI 建议入库**（兜底不卡流程）

### 3.2 旅程 B：每晚反思（22:00 推送） ⚠️ [2026-05-28 已废弃]

> 整个本节定义的「每晚反思」流程在 2026-05-28 全量下线（页面 / cron / lib / 数据表均移除）。
> 章节保留供决策回顾，**不要按此实现**。

```
22:00  → 微信 / 飞书 / Web push 推送一条
         "你今天留下了 X 道痕迹，想看看吗"
         ↓
用户点开 → AI 已经基于今天的内容自动选了反思形态（三选一）：
         ┌──────────────────────────────────────────┐
         │ • 日记态：内容散乱 / 情绪化 / 量少时       │
         │ • 学习卡：同主题密集 / 工作学习类多时       │
         │ • 教练态：检测到反复模式 / 偏离过往判断时   │
         └──────────────────────────────────────────┘
         报告末尾插一个「上周的你」模块（彩蛋）
         ↓
用户读完 → 可以下面回一句「这个想法我现在变了」
         AI 把回复也存进 Curio
```

**关键设计**：
- AI 自动选反思形态，**用户不需要选**
- 三态之间用户随时可以手动切（但不强求）
- 如果今天 < 3 条捕获，不生成完整反思，只发一句"今天比较安静，good night"

### 3.3 旅程 C：回看与搜索（不定时）

```
情境 1 ：想找资料
"我上周看到的那个关于 onboarding 的图在哪？"
→ 自然语言搜索框
→ 返回相关 items + 高亮匹配
→ 右侧可跳"这个主题下还有 X 条"

情境 2 ：想看自己在 X 主题的沉淀
点开"主题集合"页 → 选一个主题
→ 显示这个主题的：
  • 总条数 + 时间跨度
  • 关键词云
  • 时间线视图（按月聚合）
  • AI 自动写的"你对这个主题的认知演变"小结

情境 3 ：周末/月初回望
→ 周报推送（周日 21:00）
→ 月报推送（每月 10 日 21:00）
  • 主题分布饼图
  • 兴趣迁移热力图（成长轨迹）
  • 观点对比："你 3 个月前认为 X，现在认为 Y"
```

---

## 4. 关键设计原则（产品宪法）

不可违背的 6 条。设计争论时回到这 6 条解决。

### 4.1 复利感优先

每个功能上线前问：**这怎么让用户感觉到自己在长进？** 如果没答案，砍。

具体落地：
- 主题集合页**必须**有"演变小结"
- 反思报告**必须**带「上周/上月/去年的你」
- 搜索结果**必须**显示这个主题下还有多少条
- 首页**必须**有"本月你最关注的 3 个主题"

### 4.2 最低摩擦捕获 + 1 句话兜底意图

AI 能从图/截图猜到很多，但「你为什么记」AI 猜不准。所以：

- 主流：扔进去 → 1 句话批注框 / 录音 1 秒 → 入库
- 兜底：什么都不说也能入库（AI 全猜）
- 不接受：要求用户必须填字段、必须选分类、必须打 tag

### 4.3 AI 主权 + 用户兜底权

主题的命名权、合并权、拆分权交给 AI。但用户随时能改且 AI 必须记住偏好。

- AI 自动命名主题：「AI 产品交互」「字体搭配」「初创团队管理」
- AI 自动合并/拆分主题（每周末做一次主题维护）
- 用户能改主题名 → AI 下次跟上
- AI 改动主题时，反思报告里**必须**透明说明：「我把 A 和 B 合并成 C 了，因为它们 30 天来相似度 87%」

### 4.4 透明 AI（避免黑箱焦虑）

任何 AI 自动操作，用户都要能"点开看为什么"。

- AI 打的主题标签：可点击查看"为什么是这个标签"
- AI 自动合并主题：必须解释合并依据
- AI 推「上周的你」：必须显示原始来源条目

### 4.5 反 Notion 坟场

宁可少功能，不让用户感觉"东西越来越多越焦虑"。

- 不做：复杂的文件夹/嵌套结构、双向链接图谱、可视化白板
- 做：自动分类、自动反思、自动唤醒、自动总结
- 总原则：**用户负责扔，Curio 负责整**

### 4.6 长期数据资产

V0 就要做好数据导出、备份、隐私 —— 这是个人 6 个月 / 长期产品，不是 PoC。

- 数据归属：用户的，能整体导出（JSON + 图片包）
- 加密：传输 TLS + 数据库 at-rest 加密
- 隐私边界：AI 处理的 prompt **不带用户身份信息**

---

## 5. 功能拆解（V0 → V2）

### V0 · 2-3 周 · "能用 + 验证假设"

目标：能跑通 1 个完整循环（捕获 → AI 整理 → 反思 → 搜索），让作者本人能用 1-2 个月真实验证「是否真的产生复利感」。

| 模块 | 必做 | 不做 |
|---|---|---|
| 账号 | Supabase magic link 邮箱登录 | 多用户/团队/付费 |
| 捕获 | PWA 内：拍照/上传图/输入文字/录音；iOS Shortcuts 模板 | 飞书 bot、视频、网页全文抓取 |
| AI 处理 | OCR、内容理解、主题建议、1 句简介 | 自动跨条目关联 |
| 主题管理 | AI 自动命名 + 用户能改名；不做合并/拆分 | 主题智能合并（V0.5） |
| 列表 | 时间线 + 主题筛选 | 多视图、看板 |
| 搜索 | 关键词 + pgvector 语义搜索 | 高级筛选、问答式搜索（V1） |
| 反思 | 每晚 22:00 生成日记态反思，web 内查看 | 三态切换、推送通知 |
| 复利可视化 | 主题集合页（总条数 + 时间线列表） | 演变小结、成长轨迹图（V0.5） |
| 隐私 | 单用户 + RLS + 数据导出 | 端到端加密 |

**V0 死线判断**：本人用满 1 个月后还想用，进 V0.5；觉得鸡肋，回到设计阶段。

### V0.5 · 第 4-5 周 · "复利感闭环"

加入让产品**真的不一样**的部分：

- 反思三态：AI 自动选 + 用户可切（学习卡 / 教练态）
- 「上周的你」模块插入反思报告末尾 ✅（已通过 cross_period 实现）
- 主题集合页加"演变小结"（AI 写一段你这个主题的认知演变）
- 周报：每周日 21:00 推送
- AI 主题维护：每周末后台跑一次，合并/拆分主题，反思报告说明

#### V0.5 新增（5/26 借鉴 Moveworks / Rovo / Linear）

- **主动联想推送** · 1 天 · ⭐⭐⭐
  - 借鉴：Rovo Proactive + Moveworks Agentic 主动性
  - 每次新增 item 后，AI 后台跑 embedding 搜过去最相似 5 条 → 弹 toast "你 X 天前也写过相关的"
  - 触发点：捕获确认入库后；主页打开时（如果有未消化的联想）
  - 体感：让 Curio "活" 起来，AI 主动牵线而不是被动等查

- **多步 Agentic 问答** · 2 天 · ⭐⭐
  - 借鉴：Moveworks Agentic Reasoning Engine
  - 当前搜索是 1 步（embedding → 综合）；升级成 ReAct / function-calling 多步规划
  - 例："找出我所有关于 X 的思考，跟 Y 对比，告诉我视角怎么变"
  - AI 自己拆步骤：search_topic(X) → search_topic(Y) → cross_compare(items_X, items_Y) → 生成对比叙事
  - 入口：Library 搜索框（默认走单步，prompt 复杂时自动升级多步）

### V1 · 1-2 个月后 · "成长可视化 + 主动唤醒"

- 成长轨迹图（兴趣热力图 + 月度迁移）
- 月报（每月 10 号推送）
- Web push 通知（PWA 内）
- 自然语言问答式搜索 ✅（已在 V0 实现 Perplexity 风格）
- iOS Shortcuts 一键导入模板（提供 .shortcut 文件）
- 飞书 Bot 兜底捕获（如果发现工作场景捕获不够顺）

#### V1 新增（5/26 借鉴 Rovo Teamwork Graph）

- **兴趣图谱（Knowledge Graph）** · 1 周 · ⭐⭐⭐ · 差异化核心
  - 借鉴：Rovo Teamwork Graph
  - 节点 = 关键概念 / topic / 高频词；边 = "常一起出现" / "时间相邻" / "AI 推断有关联"
  - 新增表 `knowledge_nodes` + `knowledge_edges`
  - AI 后台任务：每周末根据所有 items 重建 graph
  - 前端：/graph 页 + force-directed 可视化（D3 / react-flow）
  - 节点可点击 → 跳到包含该概念的所有 items
  - 北极星价值：用户**真的看到自己的思维地图在变化**，Curio 跟其他 PKM 的关键差异化

### V1 · 1-2 个月后 · "成长可视化 + 主动唤醒"

- 成长轨迹图（兴趣热力图 + 月度迁移）
- 月报（每月 10 号推送）
- Web push 通知（PWA 内）
- 自然语言问答式搜索（"我所有关于 onboarding 的笔记里，最近的观点是什么？"）
- iOS Shortcuts 一键导入模板（提供 .shortcut 文件）
- 飞书 Bot 兜底捕获（如果发现工作场景捕获不够顺）

### V2 · 商业化前置

- 多用户 + 订阅（Supabase Auth + Stripe）
- 团队/分享版本：可选公开某个主题集合
- iOS 原生 App（Capacitor 包装 PWA，复用整个前端）
- Android PWA share target（Android 已支持）
- 高级 AI：用 Claude / GPT 处理"教练态"反思（双模型分级）

---

## 6. 信息架构与数据模型

### 6.1 核心实体

```
User
 ├── Item（每条捕获）
 │    ├── id, user_id, created_at
 │    ├── source_type: image / screenshot / text / voice / link
 │    ├── raw_content: 图片 URL / 文本 / 语音 URL
 │    ├── ocr_text / transcript_text（图/语音转写）
 │    ├── user_note: 用户的 1 句话批注
 │    ├── ai_summary: AI 一句简介
 │    ├── ai_intent: AI 推测的"为什么记"
 │    ├── embedding: vector(1536)
 │    └── topic_id（多对一）
 │
 ├── Topic（主题集合）
 │    ├── id, user_id, name（AI 命名）
 │    ├── slug, description, color
 │    ├── ai_evolution_summary: AI 写的"认知演变"
 │    ├── created_at, updated_at
 │    └── 物化字段：item_count, last_item_at
 │
 ├── Reflection（每日反思）
 │    ├── id, user_id, date
 │    ├── mode: journal / learning_card / coach
 │    ├── ai_choice_reason: AI 为什么选这个模式
 │    ├── content（结构化 JSON：sections）
 │    ├── lookback_module: 「上周的你」引用的 item_ids
 │    └── user_response: 用户的回应（可选）
 │
 └── TopicMaintenanceLog（主题合并/拆分记录）
      ├── id, user_id, action: merge / split / rename
      ├── before / after 主题快照
      ├── ai_reason
      └── created_at
```

### 6.2 关键决策

- **Item ↔ Topic 多对一（V0）**：一条只属于一个主题，简化心智
- **未来可改成多对多**（V1）：当用户开始要"这条既在 A 主题也在 B 主题"
- **Embedding 用 1536 维**（兼容 OpenAI text-embedding-3-small；如果用国内 embedding 改维度）
- **AI 字段全部存原始 prompt + response**：方便 debug + 后续模型升级回填

---

## 7. AI 系统设计

### 7.1 AI 任务清单

| 任务 | 触发时机 | 模型 | 输入 | 输出 |
|---|---|---|---|---|
| OCR + 图理解 | 图片上传后 | **DeepSeek-VL** | 图片 | {text, summary, suggested_topic, intent} 一次调用 |
| 语音转写 | 录音上传后 | **DeepSeek ASR** | 音频 | 文本 |
| 内容理解 + 主题建议 | OCR/转写完成后 | DeepSeek V4 Flash | 原始内容 + 用户批注 + 历史主题列表 | {summary, suggested_topic, intent} |
| 一键确认入库 | 用户点击确认后 | — | — | 写入 DB |
| 每日反思 | 每天 22:00 cron | DeepSeek V4 Pro（reasoning） | 当天所有 items + 最近 30 天主题分布 | {mode, content, lookback_refs} |
| 主题演变小结 | 主题页打开时按需生成（缓存） | DeepSeek V4 Pro | 该主题所有 items 按时间排 | 一段 200-400 字的"演变叙事" |
| 主题维护（合并/拆分） | 每周日凌晨 cron | DeepSeek V4 Pro | 所有主题 + items 摘要 | actions list |
| 周报 / 月报 | 周日 21:00 / 每月 10 日 21:00 | DeepSeek V4 Pro | 时间段内的 items + 主题变化 | 结构化报告 |
| 语义搜索 | 用户搜索时 | 阿里 DashScope text-embedding-v3 + pgvector | 查询文本 | 排序好的 items |

### 7.2 三态反思的判断逻辑（V0.5）

AI 看当天数据，按下面优先级判断：

```
IF 检测到"反复模式" (e.g., 同主题连续 7 天 / 与历史观点冲突)
  → mode = coach（教练态）
ELIF 同主题密集（70%+ 条目在 1-2 个主题）AND 内容偏专业/结构化
  → mode = learning_card（学习卡）
ELSE
  → mode = journal（日记态）
```

判断依据要写在 `ai_choice_reason` 里，用户能看到「为什么今晚是教练态」。

### 7.3 Prompt 工程注意点

- **不要把用户身份信息丢给 LLM**：用 user_id 哈希
- **历史上下文裁剪**：超过 30 天的 items 只传 embedding 摘要
- **温度**：分类/标签用 0.2；反思/演变小结用 0.6-0.7（要有人味）
- **失败兜底**：DeepSeek reasoning 模型 max_tokens 必须给 2000+（已知坑，记忆有记录）

---

## 8. 技术架构

### 8.1 选型表

| 层级 | 选择 | 理由 |
|---|---|---|
| 前端 | Next.js 16 + React 19 + Tailwind 4 | 与 GYMini / Talent Scope 一致，复用熟练度 |
| UI | shadcn/ui（按需）+ 自写组件 | 不要默认模板感（参考全局 design-quality 规则） |
| PWA | next-pwa + manifest.json | iOS 16.4+ 网页 push 支持 |
| 状态 | Zustand（持久化）+ TanStack Query | 区分客户端 / 服务端状态 |
| 后端 | Vercel Serverless / Edge Functions | 部署一致 |
| 数据库 | Supabase Postgres + pgvector | RLS 一开始就上、向量搜索原生支持 |
| 认证 | Supabase Auth（magic link 邮箱） | 简单、为 V2 多用户预留 |
| 存储 | Supabase Storage（图/音频） | 同生态、有 CDN |
| AI · 文本 | DeepSeek V4 系列 | 主理解 + 叙事生成 + 主题维护 |
| AI · 视觉 | **豆包 Seed-2.0-Pro**（火山引擎，5/25 切换） | OCR + 图像理解 + 主题建议 一个接口；DeepSeek API 暂不支持图像；备选 Qwen3-VL-Plus（同 DashScope key） |
| AI · 语音 | DeepSeek ASR / 火山引擎 ASR | 哪个先支持先用哪个 |
| Embedding | 阿里 DashScope text-embedding-v3（1024 维） | 国内服务、无需外币付款方式、成本几乎可忽略（¥0.02/月） |
| Cron | Vercel Cron | 跑 22:00 反思、周报、月报 |
| 监控 | Vercel Analytics + Sentry | 标配 |

### 8.2 关键架构图（文字版）

```
[ iPhone 相册/微信/任意 app ]
            │
            │ 分享 → iOS Shortcut（HTTPS POST）
            ↓
[ PWA 前端（Next.js） ] ←─ 添加到主屏幕 ───┐
            │                              │
            ↓                              │
[ Vercel Edge Functions (API) ] ───────────┘
            │
            ├──→ Supabase（Postgres + Storage + Auth）
            │         └─ pgvector 索引
            │
            ├──→ DeepSeek API（理解 / 反思 / 搜索增强）
            │
            └──→ Whisper / OCR API
            
[ Vercel Cron ] ─→ 22:00 反思 / 周报 / 月报 / 主题维护
            │
            └──→ Web Push（PWA notification）
                 + Email（备用）
```

### 8.3 部署

- 主域名：`curio.virxact.com` 或 `curio.yuri.so` 之类（建档时未定）
- 数据备份：Supabase 自带每日备份 + 手动 `pg_dump` 月度导出存本地
- 环境分离：production / preview（PR）/ local

### 8.4 与 V1 商业化的预备

- **多租户 ready**：Supabase RLS 从 day 1 启用，每张表都按 `user_id` 隔离
- **付费 ready**：用户表预留 `subscription_tier` 字段，V0 全员 free
- **API 层抽象**：所有外部 API 调用走自己的 `lib/ai-client.ts`，方便换模型/加缓存

---

## 9. UX / UI 原则

### 9.1 语气：理性、专业、「外脑」感

参照：Bear / iA Writer / Tana / Linear。

- 不写"小可爱""棒棒哒"
- AI 反思第一人称用「Curio」而不是「我」
- 用词偏书面但不学究："今日轨迹"、"主题集合"、"演变小结"
- 报告标题不带 emoji

### 9.2 视觉

按照全局 web/design-quality.md「反 template 政策」执行。

### 9.2.1 风格代号

**Dark Editorial Collection · 暗色编辑收藏**

参照定稿 mockup：`mockups/mockup-a-v3-dark-editorial.html`

气质坐标：
- 像 Apple Music 暗模式 / Apple Books / Mymind 暗模式
- 不像 Notion / 不像 Linear 仪表盘 / 不像 fintech dashboard
- 翻看自己收藏馆的"私人月刊"感

### 9.2.2 色板（纯绿色系）

```css
/* 背景 mesh gradient */
--c-bg-deep:   #0A0F0C;
--c-bg-1:      #0F1612;
--c-bg-2:      #14211B;
--c-bg-3:      #1E3328;

/* 卡片层 */
--c-card:      #16201C;
--c-card-2:    #1A2520;
--c-card-3:    #1F2A25;
--c-glass:     rgba(255, 255, 255, 0.04);
--c-border:    rgba(255, 255, 255, 0.06);

/* 主 accent */
--c-lime:      #B0F263;  /* 主调色，按钮、数字、关键 tag */
--c-lime-soft: rgba(176, 242, 99, 0.12);

/* 副 accent（仅作层次区分） */
--c-forest:    #4D7A6E;  /* 头像渐变末端、timeline dim 柱 */

/* 文字 */
--c-ink:       #FFFFFF;
--c-ink-2:     #B3BAB5;  /* 副标题、说明文字 */
--c-ink-3:     #6B736E;  /* 弱辅助、小字 byline */
--c-ink-4:     #4A5450;  /* 最弱、占位 */
```

**禁用色**：橙色 / 桃色 / 紫罗兰 / 暖色 / 蓝色。一切色块都必须是绿色系。

### 9.2.3 字体

```css
/* 主字体 */
font-family: 'Inter', -apple-system, 'PingFang SC', sans-serif;

/* 衬线点缀（仅用于斜体关键词，如「好奇」「学习卡」「产品」） */
font-family: 'Fraunces', 'Times New Roman', serif;
font-style: italic;
```

字重使用：标题 800-900、副标 600-700、正文 400-500、tag/小字 700-800 letter-spacing 0.25em。
字距：大字标题 `-0.03em`，正文 `0`。

### 9.2.4 形状 / 圆角

- 大卡片 `24px`
- 中卡片 `16px`
- pill `999px`
- 头像 `50%`
- **禁用**：octagon、hexagon、菱形等几何象形（被验证为"为象形而象形"）

### 9.2.5 关键组件

| 组件 | 规格 |
|---|---|
| 主页主题列表卡 | 圆角 16，番号 01-NN + 大字主题名 + meta byline + 大数字 lime |
| 首页"特色主题"卡 | lime 实底 + 黑字（feature 第一张） |
| Floating 捕获按钮 | lime pill + 黑色 plus 圆，发光 shadow |
| 反思 pullquote | 左侧 2px lime 竖线 + Fraunces italic |
| Lookback 卡（上周的你） | 渐变绿暗底 + 1px lime 透明边 + 顶部 2px lime 渐变线 |
| 主题大数字 | 64px Inter 900 lime + tabular-nums |
| 月度柱状图 | 当月 lime / 过往月 forest dim |
| 关键词 chip | 默认玻璃 + 边线；高频用 lime 实底黑字 |

### 9.2.6 装饰

- 顶部 mesh 高光：`radial-gradient(rgba(176,242,99,0.18))` + blur 40px
- 阴影：暗色环境下用大阴影 + 1px 微边线代替柔阴影

### 9.3 首屏（PWA 打开第一眼）

```
┌─────────────────────────────────────┐
│  Curio                              │
│  a home for everything you're       │
│  curious about.                     │
│                                     │
│  [大按钮：扔点东西进来]              │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐               │
│  │📷│ │📋│ │✍️│ │🎤│  (四个捕获入口) │
│  └──┘ └──┘ └──┘ └──┘               │
│                                     │
│  本月你最好奇的 3 件事               │
│  • AI 产品交互（23 条）              │
│  • 字体搭配（11 条）                 │
│  • 初创团队管理（9 条）              │
│                                     │
│  最近 7 天的轨迹 ▶                  │
└─────────────────────────────────────┘
```

### 9.4 反思报告页（关键页）

```
┌─────────────────────────────────────┐
│  今晚的 Curio                       │
│  5 月 24 日 · 学习卡态               │
│  ─────────────────────────         │
│  (AI 说明) "你今天 6 条里 5 条都在  │
│   讨论 onboarding 设计，我给你做了   │
│   一张学习卡。"                      │
│                                     │
│  [展开学习卡 ↓]                     │
│                                     │
│  ─── 上周的你 ───                   │
│  4 月 12 日你写过：「不要做大而全」  │
│  今天你记的方向有点偏大而全，看看？  │
│  → [打开那条]                        │
└─────────────────────────────────────┘
```

### 9.5 主题集合页

```
┌─────────────────────────────────────┐
│  ← AI 产品交互                       │
│  23 条 · 4 月 8 日 至今              │
│                                     │
│  [演变小结]                         │
│  最早你关注的是 ChatGPT 的多轮对话   │
│  设计；4 月底转向了引导式 onboarding；│
│  5 月起开始思考 agent UI 的反馈机制。│
│                                     │
│  [时间线 · 月聚合]                  │
│  ▓▓▓▓▓ 5 月 (12)                    │
│  ▓▓▓ 4 月 (9)                       │
│  ▓ 3 月 (2)                         │
│                                     │
│  [关键词云]                         │
│  onboarding · ai · 引导式 · 反馈     │
└─────────────────────────────────────┘
```

---

## 10. 关键风险与对策

| 风险 | 严重度 | 对策 |
|---|---|---|
| AI 主题质量差，用户失信任 | 高 | V0 用真实数据 dogfood 1 个月；准备"用户改名 → AI 跟上"机制；主题维护日志透明 |
| 捕获摩擦太大，用户用回相册 | 高 | iOS Shortcuts 模板做好；首次安装强制设置 + 教程；批注框 1 秒能跳过 |
| 反思报告写得水 | 高 | DeepSeek V4 Pro reasoning + 严格 prompt；少量 3 条以下不生成 |
| 推送频率不对 | 中 | V0 默认 22:00 推；连续 3 天不点开自动降频；用户可手动设置 |
| DeepSeek 数据合规疑虑（个人隐私） | 中 | Prompt 不带身份信息；V0 接受 DeepSeek，V2 商业化前评估双模型分级 |
| 长期数据膨胀，搜索变慢 | 中 | 30 天以上 items 用 embedding 摘要替代全文上下文；定期 vacuum |
| 一个人用产品，没人监督质量 | 中 | 强制要求作者每周写一次"Curio 体验记"作为产品验证 |
| 写出"又一个 Notion-like" | 高 | 视觉风格 + 反思仪式 + 复利可视化 三件事必须有别于通用笔记软件 |

---

## 11. 度量指标

V0 期间，作者本人是唯一用户，但还是要量化跟踪：

### 11.1 行为指标

| 指标 | 目标 | 怎么测 |
|---|---|---|
| 日均捕获条数 | ≥ 3 | 数据库 count |
| 每日反思打开率 | ≥ 60% | 推送 + 点击事件 |
| 周报打开率 | ≥ 80% | 同上 |
| 搜索使用频率 | ≥ 2 次/周 | 后端日志 |
| 主题集合页打开率 | ≥ 3 次/周 | 同上 |

### 11.2 质量指标

| 指标 | 目标 | 怎么测 |
|---|---|---|
| AI 主题建议接受率 | ≥ 70% | 用户点"换主题"的比例 |
| 反思报告"有用"反馈 | ≥ 4/5 平均分 | 每次报告末尾一个 1-5 分滑块 |
| 主题维护操作"接受率" | ≥ 80% | 用户对 AI 合并/拆分点"撤销"的比例 |

### 11.3 北极星验证（3 个月后）

```
作者本人回答：
"过去 3 个月，你能不能脱口而出『你在 X 主题上明显比 3 个月前想得更深了』，
 并且能指出 Curio 里的具体证据？"
```

如果答案"能" → V0.5 → V1。如果答案"不能" → 回头审产品价值假设。

---

## 12. 实施路线图

### V0 · Sprint 0（第 1 周）：地基

- [ ] Supabase 项目建好（DB + Auth + Storage + RLS）
- [ ] Next.js 项目初始化 + PWA 配置 + 基础布局
- [ ] 数据模型建表（migration 文件）
- [ ] DeepSeek client 封装（`lib/ai-client.ts`）
- [ ] Magic link 登录跑通
- [ ] 部署到 Vercel preview 域名

### V0 · Sprint 1（第 2 周）：捕获 + AI 处理

- [ ] 捕获入口 4 个：拍照 / 上传图 / 文字 / 录音
- [ ] OCR API 接通
- [ ] 语音转写 API 接通
- [ ] AI 主题建议 + 1 句简介（DeepSeek Flash）
- [ ] Item 入库 + 列表展示（时间线）
- [ ] 用户改主题名 + AI 学习偏好
- [ ] iOS Shortcuts 模板做出来，作者本人试用 1 天

### V0 · Sprint 2（第 3 周）：反思 + 搜索

- [ ] Vercel Cron 跑通 22:00 反思生成
- [ ] 反思页 UI（V0 只做日记态）
- [ ] pgvector 索引 + 语义搜索 API
- [ ] 搜索页 UI（输入框 + 结果列表）
- [ ] 主题集合页（V0 只做"条数 + 时间线"，不做演变小结）
- [ ] 数据导出（JSON dump 接口）
- [ ] 投到 production 域名

### V0 dogfood · 第 4-8 周

作者自用 4 周，每周末写一次"Curio 体验记"，记录：
- 捕获顺不顺
- AI 主题准不准
- 反思有没有打开
- 有没有真感觉到复利

第 8 周末做决策：进 V0.5 还是回头改设计。

### V0.5（第 9-10 周，若进入）

- 三态反思（学习卡 / 教练态）
- 「上周的你」模块
- 主题集合页加演变小结
- 周报
- AI 主题自动维护

### V1（V0.5 验证后 1-2 个月）

- 成长轨迹图 / 兴趣热力图
- 月报
- Web Push
- 自然语言问答搜索
- 飞书 Bot（如果工作场景用得不够）

---

## 13. 待澄清 / 待决策

下面这些是写 spec 时悬挂的、需要继续讨论的：

1. ~~PRD_AI_Knowledge_Exocortex_v1.pdf~~ —— ✅ 用户已删除，不再参考
2. **域名**：`curio.virxact.com` / `curio.yuri.so` / 别的？
3. ~~语音转写选型~~ —— ✅ 已定 DeepSeek ASR（5/24）
4. ~~OCR 选型~~ —— ✅ 已定 DeepSeek-VL（5/24）
5. **主题数量上限**：V0 不设上限；如果失控可能要在 V0.5 设个软上限（比如 50 个，超过 AI 强制合并）
6. **反思推送通道**：Web Push（需要用户授权）vs 微信服务号 vs 飞书 bot，V0 先做 Web 内查看，推送通道 V0.5 再加
7. ~~视觉风格定稿~~ —— ✅ 已定 Dark Editorial Collection · 纯绿色系（5/24，见第 9.2 节）

---

## 14. 这份 spec 的"宪法地位"

- 这是 v1.0，会随实施过程演进
- 任何改动**核心原则（第 4 节）** 或 **数据模型（第 6 节）** 的决定，要更新此文档
- V0 / V0.5 / V1 / V2 边界一旦确定不轻易移动，要移动需要重新讨论假设
- 这份文档的所有功能描述里，**当文字和图示冲突时，以"产品宪法"为准**

---

## 15. 重大调整记录

### 15.1 · 2026-05-25 · 主页从「主题集合」改为「叙事主页」

**触发**：Yuri Phase 1b 测试豆包后反馈
> "目前我扔进来内容，他形成了一个个小的卡片主题，这样可阅读性很差的，还是一个个单独的小知识点，我要每次点开读很麻烦的，AI 并没有帮我提升我的效率只是做了记录整理归纳。"
>
> "根本不要『主题』这个层 —— 直接给我『今天 / 本周 你思考了什么』的叙事"

**根因**：主题列表式主页 = Notion 坟场雏形，AI 价值停留在「单条整理」层。

**决策**：
- **主页 hero 改为「最近 7 天叙事」**：AI 写一段 200-400 字叙事文 + 主线索 + 反思盲点
- **折叠区「本月叙事」**：用户主动展开看
- **主题降级为内部分类工具**：item 仍归到 topic（AI 用来辨识），但前端**不展示主题列表**
- 叙事生成时机：**1 小时缓存 + 条数变化触发重生**（getOrGenerateNarrative）
- 模型：DeepSeek V4 Pro（reasoner）写叙事，质量优先

**新增**：
- 表：`narratives`（`narratives.sql`，0002 migration）
- 模块：`lib/narratives/{generate,queries}.ts`
- 组件：`HomeClient` 整个重写为叙事主页

**影响 SPEC 章节**：第 3 节用户旅程 / 第 9 节 UI 都需要重写。这次只先记决策，下次大改时统一更新这些章节。

### 15.2 · 2026-05-25 · 视觉模型从 DeepSeek-VL 切到豆包 Seed-2.0-Pro

**触发**：DeepSeek chat completions API 不支持 `image_url` 输入（DeepSeek-VL 模型 ID 不存在）。

**过渡**：先用 Qwen3-VL-Plus（DashScope 同账号）跑通，验证可用。然后切豆包 Seed-2.0-Pro 看效果。

**最终决策**：豆包，"质量够好、慢一点可接受"。

**实现**：
- `lib/ai/doubao-vl.ts` 主用（火山 ARK 兼容 OpenAI 模式）
- `lib/ai/qwen-vl.ts` 保留作为 fallback（一行 import 切换）
- 环境变量：`ARK_API_KEY` / `ARK_BASE_URL` / `ARK_VL_MODEL=doubao-seed-2-0-pro-260215`

---

*建档：Yuri × Claude · 2026-05-24 · 五轮采访共创*
